# vendor/*.tgz is an npm dependency committed byte-for-byte (see CLAUDE.md → "The vendored
# xlsx tarball"). Git already infers "binary" from the gzip header, but core.autocrlf=true on
# a Windows checkout is one misdetection away from corrupting an archive that nothing would
# notice until `npm ci` failed its integrity check. Say it explicitly.
*.tgz binary
